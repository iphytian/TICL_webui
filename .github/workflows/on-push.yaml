name: on-push
run-name: On push to main

on:
  # run on commit to main branch
  push:
    branches:
      - main

  # run manually by user
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-site:
    # 构建时自动运行 cite.py 生成最新论文数据，无需额外提交步骤
    uses: ./.github/workflows/build-site.yaml
